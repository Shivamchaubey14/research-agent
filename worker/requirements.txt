anthropic>=0.69.0
pydantic>=2.0
python-dotenv==1.0.1
# The worker reuses the backend's Django ORM models (single source of truth for
# the schema) and the shared messaging/streaming helpers, so it pulls in the
# same Django + driver + Kafka/Redis stack as the API.
Django==5.0.6
# The worker loads the backend's Django settings, which register these apps in
# INSTALLED_APPS/MIDDLEWARE, so they must be importable even though the worker
# serves no HTTP.
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
PyMySQL==1.1.1
dj-database-url==2.1.0
kafka-python==3.0.7
redis==5.0.4
# RAG ingestion: Qdrant client with local fastembed embeddings (no API key) and
# pypdf for extracting text from uploaded PDFs.
qdrant-client[fastembed]==1.9.1
pypdf>=4.0
